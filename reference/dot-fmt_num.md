# Compact numeric formatter shared by the summary methods

Compact numeric formatter shared by the summary methods

## Usage

``` r
.fmt_num(x, digits = 4L)
```

## Arguments

- x:

  `numeric`.

- digits:

  `integer`. Significant digits. Default `4`.

## Value

A formatted `character` scalar, without padding.
