% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11_blup.R
\name{predict.blup_fit}
\alias{predict.blup_fit}
\title{Predict with an adaptive functional BLUP fit}
\usage{
\method{predict}{blup_fit}(object, t = object$Tn0, newdata = NULL, h = 1L, ...)
}
\arguments{
\item{object}{A \code{blup_fit} object.}

\item{t}{Numeric vector of prediction points in \eqn{[0, 1]}. Default is the
conditioning-curve design points.}

\item{newdata}{Optional numeric vector of conditioning-curve values at the
fit's design points (\code{object$Tn0}), overriding \code{object$Yn0}. Used internally
for the h-step recursion; must have length \code{object$Mn0}.}

\item{h}{Integer prediction horizon (steps ahead). Default \code{1}. For \code{h > 1},
each intermediate curve is predicted on the target grid \code{t} and fed back as
the new conditioning curve; the conditioning quantities are recomputed for
that grid using the cached adaptive bandwidths (works for both designs).}

\item{...}{Unused; for S3 compatibility.}
}
\value{
A \code{data.table} with columns \code{t}, \code{muhat} (mean estimate) and
\code{prediction} (the adaptive BLUP).
}
\description{
Evaluates the adaptive Best Linear Unbiased Predictor of the curve following
the conditioning curve at the requested prediction points. For \code{h > 1}
(h-step-ahead), each intermediate curve is predicted at the (common) design
points and fed back as the new conditioning curve, so the \code{(n_0 + i)}-th
prediction is built from the \code{(n_0 + i - 1)}-th predicted curve.
}
\seealso{
\code{\link[=blup_fit]{blup_fit()}}.
}
