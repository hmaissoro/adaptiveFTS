% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/15_save_tikz.R
\name{save_plot_tikz}
\alias{save_plot_tikz}
\title{Save a plot as a (standalone) TikZ/LaTeX figure}
\usage{
save_plot_tikz(
  plot,
  file,
  width = 7,
  height = 5,
  standalone = TRUE,
  packages = NULL,
  compile = FALSE,
  clean = TRUE,
  ...
)
}
\arguments{
\item{plot}{A printable plot object — typically a \link[ggplot2:ggplot]{ggplot2::ggplot}, a
\code{ggpubr::ggarrange} arrangement, or any object whose \code{print()} method draws
to the current graphics device.}

\item{file}{\code{character(1)}. Output path. A \code{.tex} extension is appended
when missing, and the parent directory is created if needed.}

\item{width, height}{\code{numeric(1)}. Device size in inches. Defaults \code{7}
and \code{5}.}

\item{standalone}{\code{logical(1)}. Passed to \code{\link[tikzDevice:tikz]{tikzDevice::tikz()}} as
\code{standAlone}. \code{TRUE} (default) writes a self-contained document that
\code{pdflatex} can compile directly.}

\item{packages}{\code{character} or \code{NULL}. Extra LaTeX packages forwarded to
\code{\link[tikzDevice:tikz]{tikzDevice::tikz()}} via its \code{package} argument, e.g.
\code{c(getOption("tikzLatexPackages"), "\\\\usepackage{amsmath}")}.}

\item{compile}{\code{logical(1)}. If \code{TRUE}, run \code{pdflatex} on the generated
file (requires \code{standalone = TRUE} and \code{pdflatex} on the \code{PATH}). Default
\code{FALSE}.}

\item{clean}{\code{logical(1)}. If \code{TRUE} (default) and \code{compile} ran, remove
the \code{.aux} and \code{.log} by-products.}

\item{...}{Further arguments passed to \code{\link[tikzDevice:tikz]{tikzDevice::tikz()}}.}
}
\value{
The path to the written \code{.tex} file, invisibly.
}
\description{
Renders a plot to a \code{.tex} file through \code{\link[tikzDevice:tikz]{tikzDevice::tikz()}} and, optionally,
compiles it to PDF with \code{pdflatex}. This is the package version of the
\code{plot_save_tkz()}/\code{save_tikz()} helper that is otherwise copied between
analysis projects.
}
\examples{
\dontrun{
if (requireNamespace("ggplot2", quietly = TRUE) &&
    requireNamespace("tikzDevice", quietly = TRUE)) {
  data("data_far")
  g <- ggplot2::autoplot(estimate_mean(data_far, t = seq(0.1, 0.9, 0.1)))
  save_plot_tikz(g, file = file.path(tempdir(), "mean.tex"),
                 width = 7, height = 5)
}
}

}
\seealso{
\code{\link[tikzDevice:tikz]{tikzDevice::tikz()}}, \link{adaptiveFTS-autoplot}.
}
