% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12_estimator_class.R
\name{adaptiveFTS_est}
\alias{adaptiveFTS_est}
\title{Adaptive functional time series estimator objects}
\description{
The adaptive estimators of \pkg{adaptiveFTS} (for example \code{\link[=estimate_mean]{estimate_mean()}},
\code{\link[=estimate_autocov]{estimate_autocov()}}, \code{\link[=estimate_locreg]{estimate_locreg()}}, \code{\link[=estimate_cov_segment]{estimate_cov_segment()}} and their
risk counterparts) return a \link[data.table:data.table]{data.table::data.table} carrying an extra S3
class so that dedicated \code{summary()}, \code{plot()} and
\link[ggplot2:autoplot]{ggplot2::autoplot()} methods are available. The object is
still a genuine \code{data.table}: every column and every data.table operation
behaves exactly as before.
}
\details{
Each result inherits from the shared parent class \code{adaptiveFTS_est} and from a
per-estimator subclass (\code{mean_est}, \code{mean_risk}, \code{autocov_est},
\code{autocov_risk}, \code{locreg_est}, \code{cov_segment_est}, \code{cov_segment_risk},
\code{fts_acf}). A short \code{adaptive_meta} attribute stores context used by the
methods (design type, kernel, number of curves, lag, ...).
}
\section{Subsetting caveat}{

A data.table \code{[} subset returns a plain \code{data.table} — the leading
\code{adaptiveFTS_est} class is dropped, so \code{summary()} / \code{plot()} should be called
on the estimator's fresh result. Re-tagging after a subset is possible but not
generally needed.
}

\seealso{
\code{\link[=estimate_mean]{estimate_mean()}}, \code{\link[=estimate_autocov]{estimate_autocov()}}, \code{\link[=estimate_locreg]{estimate_locreg()}},
\code{\link[=estimate_cov_segment]{estimate_cov_segment()}}.
}
\keyword{internal}
