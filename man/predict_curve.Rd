% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_predict_curve.R
\name{predict_curve}
\alias{predict_curve}
\title{Curve prediction using the Best Linear Unbiased Predictor (BLUP).}
\usage{
predict_curve(
  data,
  idcol = "id_curve",
  tcol = "tobs",
  ycol = "X",
  t = seq(0.01, 0.99, len = 99),
  id_curve_to_predict = NULL,
  bw_grid = NULL,
  common_bw = FALSE,
  center_curves = TRUE,
  correct_diagonal = TRUE,
  kernel_name = "epanechnikov"
)
}
\arguments{
\item{data}{Raw curve observations, as a \code{data.table} (or
\code{data.frame}) in long format, or as a \code{list} with one element per
curve. See \code{\link{format_data}} for the accepted layouts and for the
\code{id_curve} / \code{tobs} / \code{X} columns they are converted to.}

\item{idcol}{\code{character(1)} or \code{NULL}. Name of the column holding the
curve index when \code{data} is a single table. Must be \code{NULL} when
\code{data} is a list of curves.}

\item{tcol}{\code{character(1)}. Name of the column (or vector) holding the
observation points of the curves.}

\item{ycol}{\code{character(1)}. Name of the column (or vector) holding the
values observed at those points.}

\item{t}{A numeric vector specifying the time points at which to predict the curve \code{id_curve_to_predict}.}

\item{id_curve_to_predict}{An integer specifying the index of the curve to be predicted. Default is \code{NULL}, which considers the last curve in \code{data}.}

\item{bw_grid}{A numeric vector of bandwidth grid values for selecting optimal bandwidth parameters for (auto)covariance estimation.
Default is \code{NULL}, which sets it in the function.}

\item{common_bw}{A logical value indicating whether a single bandwidth is used
for both arguments of the (auto)covariance. Default is \code{FALSE}.}

\item{center_curves}{A logical value indicating whether the curves are centred
before smoothing. Default is \code{TRUE}.}

\item{correct_diagonal}{A logical value indicating whether the diagonal of the covariances should be corrected. Default is \code{TRUE}.}

\item{kernel_name}{A string specifying the kernel to use for estimation. Supported values are \code{"epanechnikov"}, \code{"biweight"},
\code{"triweight"}, \code{"tricube"}, \code{"triangular"}, and \code{"uniform"}. Default is \code{"epanechnikov"}.}
}
\value{
A \code{data.table} containing the predicted curve:
\itemize{
\item{\code{t} :}{ The time points at which the curve \code{id_curve_to_predict} is predicted.}
\item{\code{muhat} :}{ The estimates of the mean function.}
\item{\code{prediction} :}{ The adaptive estimates the Best Linear Unbiased Predictor.}
}
}
\description{
\strong{Deprecated.} \code{predict_curve()} is deprecated and will be removed in a
future release.
It reconstructs a curve from a block system conditioning on the neighbouring
curve and the target curve's own partial observations. For the
design-weighted, Tikhonov-regularised adaptive BLUP (one-step-ahead
prediction of the curve following a conditioning curve), use \code{\link[=blup_fit]{blup_fit()}}
with \code{\link[=predict.blup_fit]{predict.blup_fit()}}, or the one-call wrapper \code{\link[=blup]{blup()}}. Note that these
compute a different quantity, so results are not interchangeable.

This function predict a curve using the adaptive Best Linear Unbiased Predictor proposed by \insertCite{maissoro2024pred;textual}{adaptiveFTS}.
}
\references{
\insertAllCited{}
}
\seealso{
\code{\link[=blup_fit]{blup_fit()}}, \code{\link[=predict.blup_fit]{predict.blup_fit()}}, \code{\link[=blup]{blup()}}, \code{\link[=estimate_mean]{estimate_mean()}}, \code{\link[=estimate_autocov]{estimate_autocov()}}.
}
