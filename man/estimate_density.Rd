% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_density_estimator.R
\name{estimate_density}
\alias{estimate_density}
\title{Leave-one-out Parzen-Rosenblatt density estimator}
\usage{
estimate_density(
  x,
  h = NULL,
  bw_grid = NULL,
  kernel_name = "epanechnikov",
  lower = 0,
  upper = 1
)
}
\arguments{
\item{x}{Numeric vector of observation times of a \emph{single} curve
\eqn{T_{n_0,\cdot}} (not pooled across curves).}

\item{h}{Optional numeric scalar bandwidth. If supplied, the least-squares
cross-validation is skipped and this bandwidth is used directly. This is
the path used by the adaptive BLUP, where a single bandwidth selected once
on a subset of curves (see \code{\link[=get_density_optimal_bw]{get_density_optimal_bw()}}) is reused for every
curve. Default is \code{NULL}, in which case the bandwidth is selected by LSCV.}

\item{bw_grid}{Numeric vector of candidate bandwidths for the LSCV search
(used only when \code{h} is \code{NULL}). If \code{NULL} (default), a fixed log-spaced
grid close to \code{exp(seq(log(0.01), log(0.3), length.out = 30))} is used. For
Monte Carlo studies, pass a fixed grid so the cross-validation is
comparable across replications.}

\item{kernel_name}{Kernel name: "epanechnikov" (default), "biweight",
"triweight", "tricube", "triangular", or "uniform".}

\item{lower, upper}{Bounds of the domain \eqn{I}; default (0, 1].}
}
\value{
A list with:
\itemize{
\item \code{h_star}: the bandwidth used.
\item \code{bw_grid}: the candidate bandwidth grid (as supplied or defaulted).
\item \code{cv_curve}: the LSCV score per candidate bandwidth, or \code{NULL} when
\code{h} is supplied.
\item \code{kernel_name}: the kernel used.
\item \code{estimate}: the vector \eqn{\widehat g_{n_0,i}(T_{n_0,i})}, one value
per observation point, in the order of \code{x}.
}
}
\description{
Computes the leave-one-out Parzen-Rosenblatt estimator of the design density
evaluated at the observation points themselves, as required by the
independent-design weights of the adaptive BLUP.
}
\details{
For a single curve with observation times \eqn{T_{n_0,i}},
\eqn{1 \le i \le M_{n_0}}, the estimator at the observation points is
\deqn{\widehat g_{n_0,i}(T_{n_0,i}) =
      \frac{1}{(M_{n_0}-1)\,h} \sum_{j \ne i}
      K\!\left(\frac{T_{n_0,i} - T_{n_0,j}}{h}\right),}
feeding the independent-design weights \eqn{\varrho_{n_0,i} =
\{M_{n_0}\,\widehat g_{n_0,i}(T_{n_0,i})\}^{-1}}. The bandwidth \eqn{h} is
either supplied directly (via \code{h}) or selected by least-squares
(Rudemo-Bowman) cross-validation over \code{bw_grid}, which minimises an unbiased
estimate (up to an \eqn{h}-independent constant) of the density MISE:
\deqn{CV(h) = \int \widehat g_h(x)^2 dx - \frac{2}{M} \sum_i \widehat
      g_h^{(-i)}(T_{n_0,i}).}
}
