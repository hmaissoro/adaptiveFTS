% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_density_estimator.R
\name{get_density_optimal_bw}
\alias{get_density_optimal_bw}
\title{Select the design-density bandwidth on a subset of curves}
\usage{
get_density_optimal_bw(
  data,
  idcol = "id_curve",
  tcol = "tobs",
  ycol = "X",
  nsubset = NULL,
  bw_grid = NULL,
  kernel_name = "epanechnikov",
  lower = 0,
  upper = 1
)
}
\arguments{
\item{data}{Raw curve observations, as a \code{data.table} (or
\code{data.frame}) in long format, or as a \code{list} with one element per
curve. See \code{\link{format_data}} for the accepted layouts and for the
\code{id_curve} / \code{tobs} / \code{X} columns they are converted to.}

\item{idcol}{\code{character(1)} or \code{NULL}. Name of the column holding the
curve index when \code{data} is a single table. Must be \code{NULL} when
\code{data} is a list of curves.}

\item{tcol}{\code{character(1)}. Name of the column (or vector) holding the
observation points of the curves.}

\item{ycol}{\code{character(1)}. Name of the column (or vector) holding the
values observed at those points.}

\item{nsubset}{\code{integer (positive)}. The number of curves to randomly
and uniformly select for bandwidth selection. Default is \code{NULL}, in which
case every curve is used.}

\item{bw_grid}{Numeric vector of candidate bandwidths passed to
\code{\link[=estimate_density]{estimate_density()}}. Default is \code{NULL} (a fixed log-spaced grid). For
reproducible Monte Carlo studies, pass a fixed grid.}

\item{kernel_name}{Kernel name: "epanechnikov" (default), "biweight",
"triweight", "tricube", "triangular", or "uniform".}

\item{lower, upper}{Bounds of the domain \eqn{I}; default (0, 1].}
}
\value{
A \code{numeric} scalar: the median of the per-curve LSCV-optimal
bandwidths over the subset.
}
\description{
Selects a single Parzen-Rosenblatt bandwidth for the design density by
least-squares cross-validation on a subset of curves, mirroring
\code{\link[=get_nw_optimal_bw]{get_nw_optimal_bw()}}.
}
\details{
For each sampled curve the bandwidth minimising the LSCV score of
\code{\link[=estimate_density]{estimate_density()}} over \code{bw_grid} is computed, and the median of these
per-curve optima is returned. The adaptive BLUP calls this once and then
reuses the returned bandwidth for every curve (passing it as the \code{h} argument
of \code{\link[=estimate_density]{estimate_density()}}), so the design weights are consistent across curves
and the cross-validation is not repeated on every call.
}
\seealso{
\code{\link[=estimate_density]{estimate_density()}}, \code{\link[=get_nw_optimal_bw]{get_nw_optimal_bw()}}.
}
