% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11_blup.R
\name{blup_fit}
\alias{blup_fit}
\title{Fit the adaptive functional BLUP}
\usage{
blup_fit(
  data,
  idcol = "id_curve",
  tcol = "tobs",
  ycol = "X",
  kernel_name = "epanechnikov",
  homoscedastic = TRUE,
  bw_subgrid_size = 10L,
  n_cv_curves = 30L,
  id_conditioning_curve = NULL,
  tikhonov = NULL,
  tikhonov_grid = NULL,
  bw_grid = NULL,
  density_bw = NULL
)
}
\arguments{
\item{data}{Raw curve observations, as a \code{data.table} (or
\code{data.frame}) in long format, or as a \code{list} with one element per
curve. See \code{\link{format_data}} for the accepted layouts and for the
\code{id_curve} / \code{tobs} / \code{X} columns they are converted to.}

\item{idcol}{\code{character(1)} or \code{NULL}. Name of the column holding the
curve index when \code{data} is a single table. Must be \code{NULL} when
\code{data} is a list of curves.}

\item{tcol}{\code{character(1)}. Name of the column (or vector) holding the
observation points of the curves.}

\item{ycol}{\code{character(1)}. Name of the column (or vector) holding the
values observed at those points.}

\item{kernel_name}{Kernel name. Default \code{"epanechnikov"}.}

\item{homoscedastic}{If \code{TRUE} (default) a constant noise variance (median of
the pointwise estimates) is used; otherwise the t-varying estimates.}

\item{bw_subgrid_size}{Number of points per axis of the coarse sub-grid on
which the adaptive bandwidths are selected. Default \code{10}.}

\item{n_cv_curves}{Number of trailing curves held out for the Tikhonov
cross-validation when \code{tikhonov} is \code{NULL}. Default \code{30}.}

\item{id_conditioning_curve}{Integer id of the curve conditioned on. Its
successor is the curve that \code{predict()} reconstructs. Default \code{NULL} uses
the last curve in \code{data}.}

\item{tikhonov}{Tikhonov regularisation parameter \eqn{\alpha}. Default \code{NULL}
selects it by cross-validation (see \code{\link[=select_tikhonov_parameter]{select_tikhonov_parameter()}}) over
\code{tikhonov_grid}; pass a numeric value to use it directly.}

\item{tikhonov_grid}{Candidate values for the cross-validation when \code{tikhonov}
is \code{NULL}. Default \code{NULL} uses the default grid of
\code{\link[=select_tikhonov_parameter]{select_tikhonov_parameter()}}.}

\item{bw_grid}{Bandwidth grid for the adaptive mean/(auto)covariance risk.
Default \code{NULL} sets a geometric grid from the data.}

\item{density_bw}{Optional fixed design-density bandwidth reused for every
\code{estimate_density} call (independent design only). Default \code{NULL} selects it
once via \code{\link[=get_density_optimal_bw]{get_density_optimal_bw()}}.}
}
\value{
An object of class \code{blup_fit}: a list whose main elements are:
\itemize{
\item \code{Tn0}, \code{Yn0}: the conditioning-curve design points and values.
\item \code{rho}, \code{root_Dn0}: the design weights and their square-root matrix.
\item \code{muhat_Tn0}, \code{c0hat}, \code{sigma2}: the mean, covariance operator and
noise level of the conditioning curve.
\item \code{V}, \code{resid}: the regularised variance matrix and the conditioning
residual.
\item \code{opt_mean}, \code{opt_cov}, \code{opt_autocov}: the cached adaptive bandwidths.
\item \code{data}, \code{kernel_name}, \code{is_common_design}, \code{density_bw}, \code{bw_grid},
\code{tikhonov}, \code{homoscedastic}: the information needed by
\code{\link[=predict.blup_fit]{predict.blup_fit()}}.
\item \code{tikhonov_cv}: the \code{\link[=select_tikhonov_parameter]{select_tikhonov_parameter()}} output when
\code{tikhonov} was selected, otherwise \code{NULL}.
}
}
\description{
Estimates every component of the adaptive Best Linear Unbiased Predictor that
does not depend on the prediction points.
}
\details{
The fit conditions on a single curve (its immediate successor is what
\code{predict()} reconstructs) and caches the adaptive bandwidths, so that
\code{predict()} only re-runs the cheap plug-in estimates at the requested
prediction points. The covariance assembly is a single lag-1 block.
}
\seealso{
\code{\link[=predict.blup_fit]{predict.blup_fit()}}, \code{\link[=get_density_optimal_bw]{get_density_optimal_bw()}}.
}
